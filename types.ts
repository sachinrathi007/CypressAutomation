export enum AIRPORTS {
  "VIENNA" = "VIE",
  "MALTA" = "MLA",
}

export enum ROUTE {
  "ONEWAY" = "One way",
  "ROUNDTRIP" = "Round trip",
}

export enum CLASS {
  "ECONOMY" = "Economy",
  "BUSINESS" = "Business",
}
